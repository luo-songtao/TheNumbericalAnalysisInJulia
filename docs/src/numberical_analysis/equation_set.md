# 求解n元方程

```@meta
CurrentModule = EquationSet
```

## Content
```@contents
Pages = map(file -> joinpath("equation_set", file), readdir("equation_set"))
Depth = 3
```

## Index
```@index
Pages   = map(file -> joinpath("equation_set", file), readdir("equation_set"))
```

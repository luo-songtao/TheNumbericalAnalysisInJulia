# 求解一元方程

```@meta
CurrentModule = EquationSolving
```

## Content
```@contents
Pages = map(file -> joinpath("equation_solving", file), readdir("equation_solving"))
Depth = 3
```

## Index
```@index
Pages   = map(file -> joinpath("equation_solving", file), readdir("equation_solving"))
```
